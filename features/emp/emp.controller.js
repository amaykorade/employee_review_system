import mongoose from "mongoose";
import PerRepository from "./emp.repository.js";

export default class PerController{
    constructor(){
        this.perController = new PerController();
    }

    async add(req, res){
        try{
            const 
        }
    }
}